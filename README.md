# vpn_bot
Create "vpn_bot" dir

then:

git clone https://github.com/ylyas8892/vpn4tkm.git

then:

Edıt "telegram token" and "telegram ID" in Confıg.py file,

then:

chmod +x deploy.sh

then start:

./deploy.sh
